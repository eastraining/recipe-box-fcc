import React from 'react';
import { render } from 'react-dom';

// Form
// Takes an object item and two callback functions
// Displays item contents in an input form
// handleChange and handleSubmit depends on container's implementation
const Form = ({item, handleChange, handleSubmit}) => (
  <div className="contentbar">
    <input>
    </input>
  </div>
)

export default Form;
